import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        backgroundColor: 'red',
        flexDirection: 'row'
    },
    fotoAvatar: {
        width: 50,
        height: 50,
        borderRadius: 100,
    },
    texto: {
        fontSize: 25,
    }
})

export default styles;